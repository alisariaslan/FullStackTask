# --- API & GATEWAY AYARLARI ---

# Sunucu tarafı (Server-to-Server) API istekleri için kullanılan ana URL.
# 'apiRequest' fonksiyonu içinde, kod sunucu tarafında (window undefined iken) 
# çalışıyorsa bu URL üzerinden Docker veya iç ağ üzerinden haberleşir.
GATEWAY_URL=http://localhost:6004

# Tarayıcı tarafı (Browser-to-Server) API istekleri için kullanılan URL.
# 'NEXT_PUBLIC_' ön eki sayesinde tarayıcıdan erişilebilir. 
# Kullanıcının tarayıcısı üzerinden API'ye (Fetch/Axios) istek atarken bu adresi kullanır.
NEXT_PUBLIC_GATEWAY_URL=http://localhost:6004


# --- SİTE AYARLARI ---

# Sitenin kendi ana URL'i. 
# Layout.tsx içerisinde MetadataBase (SEO), Canonical URL'ler ve 
# Open Graph (sosyal medya paylaşım görselleri) oluşturulurken referans alınır.
NEXT_PUBLIC_SITE_URL=https://localhost:6005


# --- HATA YÖNETİMİ & DEBUG ---

# Sepet birleştirme (Cart Merge) hatalarının sessize alınıp alınmayacağını belirler.
# '1' ise: Login sonrası veya sepet işlemlerindeki hatalar konsola basılmaz ve 
# kullanıcıya kritik olmayan uyarılar gösterilmez (apiHandler.ts ve ProductCard.tsx içinde kullanılır).
NEXT_PUBLIC_SILENT_CART_MERGE_ERRORS=0

# (Bu dosya Gitignore bilinçli olarak eklenmemiştir )